#' @param as_plotly This logical value (default: `FALSE`) can be used to
#' create an interactive plot based on the library `plotly`
#' (see [`innsight_plotly`] for details).\cr
#' *Note:* Make sure that the suggested package `plotly` is installed
#' in your R session.
