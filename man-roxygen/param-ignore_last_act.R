#' @param ignore_last_act Set this logical value to include the last
#' activation functions for each output layer, or not (default: `TRUE`).
#' In practice, the last activation (especially for softmax activation) is
#' often omitted.
