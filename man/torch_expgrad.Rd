% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/torch_gradients.R
\name{torch_expgrad}
\alias{torch_expgrad}
\title{Direct Expected Gradients for torch models}
\usage{
torch_expgrad(
  model,
  data,
  data_ref = NULL,
  output_idx = NULL,
  n = 50,
  dtype = "float",
  return_object = FALSE
)
}
\arguments{
\item{model}{(\code{\link[torch]{nn_module}})\cr
A torch model.}

\item{data}{(\code{\link[torch]{torch_tensor}}, \code{array}, or \code{matrix})\cr
Input data.}

\item{data_ref}{(\code{\link[torch]{torch_tensor}}, \code{array}, or \code{matrix})\cr
Reference dataset for estimating conditional expectation.
If \code{NULL}, uses zeros. Default: \code{NULL}.}

\item{output_idx}{(\code{integer})\cr
Index or indices of output nodes. Default: \code{NULL} (all outputs).}

\item{n}{(\code{integer(1)})\cr
Number of reference samples and integration steps. Default: \code{50}.}

\item{dtype}{(\code{character(1)})\cr
Data type: \code{"float"} or \code{"double"}. Default: \code{"float"}.}

\item{return_object}{(\code{logical(1)})\cr
If \code{TRUE}, returns a \code{\link{TorchGradientResult}} object with
methods like \code{plot()} and \code{get_result()}. If \code{FALSE} (default), returns
a raw \code{\link[torch]{torch_tensor}}.}
}
\value{
If \code{return_object = FALSE} (default): A \code{\link[torch]{torch_tensor}}
containing the expected gradients with shape \verb{(batch_size, ..., n_outputs)}.
If \code{return_object = TRUE}: A \code{\link{TorchGradientResult}} object.
}
\description{
Calculate Expected Gradients (GradSHAP) using native torch autograd.
This function provides a lightweight alternative to \code{\link{run_expgrad}}.
}
\details{
Expected Gradients extends Integrated Gradients by averaging over multiple
reference values from a distribution:

\deqn{E_{x'\sim X', \alpha \sim U(0,1)}[(x - x') \times \frac{\partial f(x' + \alpha (x - x'))}{\partial x}]}

This provides approximate Shapley values.
}
\examples{
\dontrun{
library(torch)

model <- nn_sequential(nn_linear(10, 3))
data <- torch_randn(5, 10)
references <- torch_randn(100, 10)  # Reference distribution

# Calculate Expected Gradients
exp_grads <- torch_expgrad(model, data, data_ref = references)
}

}
\references{
G. Erion et al. (2021) \emph{Improving performance of deep learning models
with axiomatic attribution priors and expected gradients.}
Nature Machine Intelligence 3, pp. 620-631.
}
\seealso{
\code{\link{run_expgrad}}, \code{\link{ExpectedGradient}}
}
