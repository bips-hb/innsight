% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/torch_gradients.R
\name{torch_smoothgrad}
\alias{torch_smoothgrad}
\title{Direct SmoothGrad for torch models}
\usage{
torch_smoothgrad(
  model,
  data,
  output_idx = NULL,
  n = 50,
  noise_level = 0.1,
  times_input = FALSE,
  dtype = "float",
  return_object = FALSE
)
}
\arguments{
\item{model}{(\code{\link[torch]{nn_module}})\cr
A torch model.}

\item{data}{(\code{\link[torch]{torch_tensor}}, \code{array}, or \code{matrix})\cr
Input data.}

\item{output_idx}{(\code{integer})\cr
Index or indices of output nodes. Default: \code{NULL} (all outputs).}

\item{n}{(\code{integer(1)})\cr
Number of noisy samples. Default: \code{50}.}

\item{noise_level}{(\code{numeric(1)})\cr
Standard deviation of noise relative to input range:
\eqn{\sigma = (max(x) - min(x)) *} \code{noise_level}. Default: \code{0.1}.}

\item{times_input}{(\code{logical(1)})\cr
If \code{TRUE}, multiplies gradients by input (SmoothGrad×Input).
Default: \code{FALSE}.}

\item{dtype}{(\code{character(1)})\cr
Data type: \code{"float"} or \code{"double"}. Default: \code{"float"}.}

\item{return_object}{(\code{logical(1)})\cr
If \code{TRUE}, returns a \code{\link{InterpretingMethod}} object with
methods like \code{plot()} and \code{get_result()}. If \code{FALSE} (default), returns
a raw \code{\link[torch]{torch_tensor}}.}
}
\value{
If \code{return_object = FALSE} (default): A \code{\link[torch]{torch_tensor}}
containing the smoothed gradients with shape \verb{(batch_size, ..., n_outputs)}.
If \code{return_object = TRUE}: A \code{\link{InterpretingMethod}} object.
}
\description{
Calculate SmoothGrad by averaging gradients over noisy samples of the input.
This function provides a lightweight alternative to \code{\link{run_smoothgrad}}.
}
\details{
SmoothGrad computes gradients for \code{n} noisy versions of each input
and averages them. With \eqn{\epsilon \sim N(0,\sigma)}:

\deqn{1/n \sum_{i=1}^n \frac{\partial f(x+ \epsilon_i)}{\partial x}}

This reduces noise in gradient-based explanations.
}
\examples{
\dontrun{
library(torch)

model <- nn_sequential(nn_linear(10, 3))
data <- torch_randn(5, 10)

# Standard SmoothGrad
smooth_grads <- torch_smoothgrad(model, data)

# SmoothGrad×Input
smooth_grads <- torch_smoothgrad(model, data, times_input = TRUE)

# More samples for smoother result
smooth_grads <- torch_smoothgrad(model, data, n = 100)
}

}
\references{
D. Smilkov et al. (2017) \emph{SmoothGrad: removing noise by adding noise.}
arXiv:1706.03825
}
\seealso{
\code{\link{run_smoothgrad}}, \code{\link{SmoothGrad}}
}
