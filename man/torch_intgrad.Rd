% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/torch_gradients.R
\name{torch_intgrad}
\alias{torch_intgrad}
\title{Direct Integrated Gradients for torch models}
\usage{
torch_intgrad(
  model,
  data,
  x_ref = NULL,
  output_idx = NULL,
  n = 50,
  times_input = TRUE,
  dtype = "float",
  return_object = FALSE
)
}
\arguments{
\item{model}{(\code{\link[torch]{nn_module}})\cr
A torch model. Must be an instance of \code{nn_module}.}

\item{data}{(\code{\link[torch]{torch_tensor}}, \code{array}, or \code{matrix})\cr
Input data for which to calculate gradients.}

\item{x_ref}{(\code{\link[torch]{torch_tensor}}, \code{array}, or \code{matrix})\cr
Reference input (baseline). If \code{NULL}, uses zeros. Must have shape
\verb{(1, ...)} where \code{...} matches input dimensions. Default: \code{NULL}.}

\item{output_idx}{(\code{integer})\cr
Index or indices of output nodes. Default: \code{NULL} (all outputs).}

\item{n}{(\code{integer(1)})\cr
Number of steps for approximating the integral. Default: \code{50}.}

\item{times_input}{(\code{logical(1)})\cr
If \code{TRUE}, multiplies integrated gradients by \code{(data - x_ref)}.
This is the standard Integrated Gradients formulation. Default: \code{TRUE}.}

\item{dtype}{(\code{character(1)})\cr
Data type: \code{"float"} or \code{"double"}. Default: \code{"float"}.}

\item{return_object}{(\code{logical(1)})\cr
If \code{TRUE}, returns a \code{\link{InterpretingMethod}} object with
methods like \code{plot()} and \code{get_result()}. If \code{FALSE} (default), returns
a raw \code{\link[torch]{torch_tensor}}.}
}
\value{
If \code{return_object = FALSE} (default): A \code{\link[torch]{torch_tensor}}
containing the integrated gradients with shape \verb{(batch_size, ..., n_outputs)}.
If \code{return_object = TRUE}: A \code{\link{InterpretingMethod}} object.
}
\description{
Calculate Integrated Gradients using native torch autograd. This function
provides a lightweight alternative to \code{\link{run_intgrad}} that works
directly with \code{torch::nn_module} objects.
}
\details{
Integrated Gradients calculates feature importance by integrating gradients
along the path from a baseline \eqn{x'} to the input \eqn{x}:

\deqn{(x - x') \times \int_{\alpha=0}^{1} \frac{\partial f(x' + \alpha (x - x'))}{\partial x} d\alpha}

The integral is approximated using \code{n} interpolation steps.
}
\examples{
\dontrun{
library(torch)

model <- nn_sequential(nn_linear(10, 3))
data <- torch_randn(5, 10)

# Use zero baseline (default)
int_grads <- torch_intgrad(model, data)

# Use custom baseline
baseline <- torch_zeros(1, 10)
int_grads <- torch_intgrad(model, data, x_ref = baseline)

# More integration steps for higher accuracy
int_grads <- torch_intgrad(model, data, n = 100)
}

}
\references{
M. Sundararajan et al. (2017) \emph{Axiomatic attribution for deep networks.}
ICML 2017, PMLR 70, pp. 3319-3328.
}
\seealso{
\code{\link{run_intgrad}}, \code{\link{IntegratedGradient}}
}
